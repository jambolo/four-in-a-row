[workspace]
resolver = "3"
members = ["src-core", "src-tauri"]
# Vendored submodule: a path dependency under the workspace root would otherwise be
# pulled in as a member, subjecting it to our clippy/fmt/test sweeps.
exclude = ["vendor/game-player"]
